SELECT users.id, users.username, users.image, users.email, chatroom.id as chatroom_id, chatroom.image, chatroom.subject, chatroom.creator_id FROM users
JOIN chatroom on(chatroom.creator_id = users.id);